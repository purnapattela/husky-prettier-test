# husky-prettier-test

`txt
 npm create vite@latest
 echo "# husky-prettier-test" >> README.md
 git init
 git add README.md
 git commit -m "first commit"
 git branch -M main
 git remote add origin git@github.com:purnapattela/husky-prettier-test.git
 git push -u origin main 
 git add .
 git commit -m "updated README.md"
 git push
 npm init @eslint/config
 npm install --save-dev --save-exact prettier
 npm install --save-dev husky lint-staged
 npx husky install
 npm install --save-dev husky
 npx husky init
 npx husky add .husky/pre-commit
 npx husky init
 git add .
 npm install --save-dev --save-exact prettier
 node --eval "fs.writeFileSync('.prettierrc','{}\n')"
 node --eval "fs.writeFileSync('.prettierignore','# Ignore artifacts:\nbuild\ncoverage\n')"
 npm install -D eslint eslint-config-prettier lint-staged
 npm install -D eslint-plugin-react
 git add .
 git commit -m "Added configs to manage eslint husky pretttier"
 npm run lint
 npx eslint --print-config src/App.jsx
 npm run lint
 git add .
 git commit -m "Added config files for the husky, eslint, prettier"
 git push
 git add .
 git commit -m "Testing configs"
 git push
 `
